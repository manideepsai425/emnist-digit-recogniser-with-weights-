# EMNIST Recogniser — Backend Dependencies
# Python 3.11+

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Deep Learning (CPU-only for Render deployment)
# For GPU training on Colab, torch is pre-installed — no need to pip install
torch==2.5.1+cpu ; sys_platform != "darwin"
torch==2.5.1     ; sys_platform == "darwin"
torchvision==0.20.1+cpu ; sys_platform != "darwin"
torchvision==0.20.1     ; sys_platform == "darwin"

# Image processing
Pillow==11.0.0
numpy==2.1.3

# HTTP
httpx==0.28.0

# Validation
pydantic==2.10.3
