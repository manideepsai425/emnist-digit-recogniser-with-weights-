# EMNIST Recogniser — Backend Dependencies
# Python 3.11+

# Web framework
fastapi
uvicorn[standard]
python-multipart

# Deep Learning (CPU-only for Render deployment)
torch ; sys_platform != "darwin"
torch ; sys_platform == "darwin"
torchvision ; sys_platform != "darwin"
torchvision ; sys_platform == "darwin"

# Image processing
Pillow
numpy

# HTTP
httpx

# Validation
pydantic